package util

func GetMasterConfig(masterConfigFileName string) string {
	// If file is already cached locally, use that, else pull from remote
	// Could see a FileReader struct that manages all of this
	return "master config file contents"
}
